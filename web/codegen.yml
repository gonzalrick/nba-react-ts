overwrite: true
schema: http://localhost:4000
documents: ./src/graphql/*.gql
generates:
  ./src/generated/index.tsx:
    config:
      noNamespaces: true
    plugins:
      - typescript-common
      - typescript-client
      - typescript-react-apollo

