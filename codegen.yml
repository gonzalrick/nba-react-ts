overwrite: true
schema: http://localhost:4000/graphql
documents: ./web/src/graphql/*.gql
generates:
  ./web/src/generated/graphqlComponents.tsx:
    config:
      noNamespaces: true
    plugins:
      - typescript-common
      - typescript-client
      - typescript-react-apollo
