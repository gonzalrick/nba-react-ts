overwrite: true
schema: http://localhost:4000
generates:
  ./app/generated/index.ts:
    plugins:
      - "typescript-common"
      - "typescript-server"
      - "typescript-resolvers"
    config:
      contextType: ../context#IContext

